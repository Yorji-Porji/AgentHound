[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agenthound"
version = "0.2.0"
description = "Graph-based attack path mapping for AI agents, MCP servers, and the NHIs they reach"
readme = "README.md"
requires-python = ">=3.11,<3.15"
license = { text = "Apache-2.0" }
authors = [
    { name = "Yorji-Porji" },
]
keywords = ["security", "offensive-security", "bloodhound", "mcp", "ai-agent", "nhi", "attack-path"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Security",
]
dependencies = [
    "click>=8.1",
    "pyyaml>=6.0",
    "pydantic>=2.0",
    # Windows ships no system zoneinfo database; ScopeGuard time-window tz math
    # relies on it, so vendor the IANA data there.
    "tzdata; platform_system == 'Windows'",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "ruff>=0.1",
    "mypy>=1.7",
    "jsonschema>=4.20",
    "types-PyYAML>=6.0",
    "freezegun>=1.2",
]

[project.scripts]
agenthound = "agenthound.cli:main"

[project.urls]
Homepage = "https://github.com/Yorji-Porji/agenthound"
Issues = "https://github.com/Yorji-Porji/agenthound/issues"

[tool.setuptools.packages.find]
include = ["agenthound*"]

[tool.setuptools.package-data]
"agenthound.data" = ["*.yaml"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]
# UP042: the `class X(str, Enum)` pattern is deliberate — members must be
# JSON-serializable string values for OpenGraph emission and round-tripping.
# Migrating to StrEnum is a separate, behavior-sensitive change.
ignore = ["UP042"]
